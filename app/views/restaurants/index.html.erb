<nav class="navbar navbar-expand-lg navbar-light bg-light navbar-lewagon navbar-collapse">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Welcome, this is the list of restaurants!</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#"><%= link_to "Create a new restaurant", new_restaurant_path %></a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to restaurant.name, restaurant_path(restaurant) %></li>
  <% end %>
</ul>
